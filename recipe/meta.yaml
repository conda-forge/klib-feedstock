{% set name = "klib" %}
{% set version = "0.2.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: f52de4f4c8b1cd0a5651a987fc867728e945808ef3935aea72d63c0e56f90d35

build:
  noarch: python
    number: 0
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.7
    - setuptools
    - matplotlib >=3.0.3
    - numpy >=1.16.3
    - pandas >=1.1.2
    - seaborn >=0.11.1
    - scikit-learn >=0.23.1
    - scipy >=1.1.0
  run:
    - python >=3.7
    - matplotlib-base >=3.0.3
    - numpy >=1.16.3
    - pandas >=1.1.2
    - seaborn >=0.11.1
    - scikit-learn >=0.23.1
    - scipy >=1.1.0

test:
  imports:
    - klib
    - klib.tests

about:
  home: https://github.com/akanz1/klib
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: Customized data preprocessing functions for frequent tasks.
  dev_url: https://github.com/akanz1/klib

extra:
  recipe-maintainers:
    - akanz1
